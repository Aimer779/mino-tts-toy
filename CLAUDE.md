## 回复风格
- 适用于所有使用中文回复的场景
  
### 禁止的高频 AI 中文模式

1. **段末收尾总结句** - 不写 "这说明"、"可以看出"、"到这里"、"由此可见" 作为段落结尾
2. **三段式结构** - 不写 "首先...其次...最后..." 串联的排比段落
3. **升华句** - 不把具体观察拔高到普遍真理（"这体现了工程师精神" / "这就是开源的魅力"）
4. **对比框架** - 不用 "不是...而是..." 句式（尤其作为段落收尾）
5. **提示语引导** - 不写 "值得注意的是"、"需要指出的是"、"有一点很重要"
6. **报告腔** - 不用 "本次"、"整体而言"、"综上所述"、"具体来说"、"随着...的发展"
7. **形式感连接词** - 不用 "从而"、"进而"、"基于此"、"有鉴于此" 做段落过渡
8. **图后 prose 与 alt 对齐** - 图片 alt text 列了几项，正文 prose 就要展开同样几项，不能错位。改正文之前先看图 alt，改完检查图 alt，必要时图也得重画

---

## Harness 工作流

### 核心原则

不能一路黑盒干到底。每一轮都要先拿到中间产物，再决定是否继续

### 文件结构

```
harness/
├── spec.md                 # 项目规格（真相源，不超过 30 行）
├── handoff.md              # 会话交接（状态快照，不超过 20 行）
├── archive/                # 历史归档（按日期）
└── index.md                # 全局索引（一行一条）

```

### 7 个控制点

| 控制点 | 触发时机 |
|--------|---------|
| intent-align | 会话开始、新任务 |
| spec-write | 意图对齐后 | 
| handoff-recover | 新会话开始 | 
| check-before-execute | 即将修改代码 |
| redefine-goal | 执行中出问题 | 
| verify-by-evidence | 模型声称完成 |
| handoff-write | 会话结束前 | 

### 流转关系

```
会话开始
    │
    ▼
handoff-recover  → 恢复状态
    │
    ▼
intent-align  → 对齐目标
    │
    ▼
spec-write  → 压成最小 spec
    │
    ▼
check-before-execute  → 执行前 checkpoint
    │
    ▼
  执行代码
    │
    ├── 出问题 ──→ redefine-goal  → 重定义目标
    │
    ▼
verify-by-evidence  → 用证据验证
    │
    ▼
handoff-write  → 回写交接
    │
    ▼
  会话结束
```

**会话开始时**：
```
读 harness/spec.md 和 harness/handoff.md，恢复任务状态
```

**每轮执行前**：
```
先别改代码。做一次 checkpoint：当前理解、核心目标、下一步动作、风险和验证方式
```

**会话结束前**：
```
把这轮做了什么、验证了什么、还剩什么，回写到 harness/handoff.md
```

